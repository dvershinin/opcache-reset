# Directories
/.git
/.github
/.wordpress-org
/tests
/.cursor
/.pytest_cache
/.idea

# Version control / hooks metadata. Plugin Check rejects ANY hidden file in the
# shipped package ("Hidden files are not permitted"), and until 2026-08-06 both
# of these were actually being published to WordPress.org.
/.gitattributes
/.pre-commit-config.yaml
/.file-limits

# Pre-commit helper scripts. Plugin Check rejects .sh files in the shipped
# package ("Application files are not permitted"); /scripts leaked into 2.4.2.
/scripts

# Files
/.distignore
/.gitignore
/.editorconfig
/Makefile
/phpcs.xml.dist
/phpstan.neon
/phpstan-phpdoc.neon
/phpstan-bootstrap.php
/composer.json
/composer.lock
/README.md

# Agent / assistant tooling
/.claude
/CLAUDE.md
/AGENTS.md

# Build + scratch artifacts
/build
/.pytest_cache
/.playwright-cli
/.playwright-mcp
__pycache__

# SVG source files (PNGs are in .wordpress-org)
*.svg

# IDE/Editor files
*.swp
*.swo
*~
.idea
.vscode
*.code-workspace

# OS files
.DS_Store
Thumbs.db
